<!DOCTYPE html>
<html>
  <head>
    <title>旅スポ</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css2?family=Zen+Kaku+Gothic+New&display=swap" rel="stylesheet">
  </head>

  <body class="d-flex flex-column vh-100" style="background-color: #f8fbf8;">
    <header class="" style="height: 10%; background-color: #d4dcda;">
      <nav class="navbar navbar-expand-lg navbar-light h-100">
        <div class="container h-100 kiwi_kaku font-weight-bold">
          <%= link_to image_tag('logo', size: "50x50"), root_path %>
          <ul class="navbar-nav ml-auto"> 
            <li class="nav-item mr-4">
              <span><i class="fas fa-map-marker-alt"></i> <%= link_to '旅スポについて', about_path, class: "text-decoration-none text-dark" %></span>
            </li>
            <% if user_signed_in? %>
              <li class="nav-item mr-4">
                <span><i class="fas fa-user"></i> <%= link_to 'マイページ', user_path(current_user.id), class: "text-decoration-none text-dark" %></span>
              </li>
              <li class="nav-item mr-4">
                <span><i class="fas fa-sign-out-alt"></i> <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "text-decoration-none text-dark" %></span>
              </li>
            <% else %>
              <li class="nav-item mr-4">
                <span><i class="fas fa-pen-square"></i> <%= link_to '新規登録', new_user_registration_path, class: "text-decoration-none text-dark" %></span>
              </li>
              <li class="nav-item">
                <span><i class="fas fa-sign-in-alt"></i> <%= link_to 'ログイン', new_user_session_path, class: "text-decoration-none text-dark" %></span>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
